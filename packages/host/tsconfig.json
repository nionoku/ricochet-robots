{
  "extends": "../../tsconfig.json",

  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "#app-config": [
        "../../config/app-config.json"
      ]
    }
  },

  "include": [
    "**/*.ts",

    "../../env.d.ts"
  ]
}
