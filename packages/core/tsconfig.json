{
  "extends": "../../tsconfig.json",

  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "#app-config": [
        "../../config/app-config.json"
      ]
    }
  },

  "include": [
    "src",

    "vite.config.ts",
    "../../env.d.ts"
  ]
}
