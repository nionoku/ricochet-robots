{
  "name": "core",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "npm run lint:types && vite build",
    "lint:types": "tsc",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/three": "^0.176.0"
  },
  "dependencies": {
    "three": "^0.176.0"
  }
}