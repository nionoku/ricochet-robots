{
  "name": "core",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "npm run lint && vite build",
    "lint": "npm run lint:eslint && npm run lint:types",
    "lint:eslint": "npx eslint",
    "lint:types": "tsc"
  },
  "devDependencies": {
    "@types/three": "^0.176.0"
  },
  "dependencies": {
    "three": "^0.176.0"
  }
}